run_group: latest
save_group: latest

wandb:
  use_wandb: True
  project: mw_harl_new

steps: ???
hydra:
  run:
    dir: ./results/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ./results/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}